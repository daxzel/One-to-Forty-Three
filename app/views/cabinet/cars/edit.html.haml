%h1 Editing car

= render 'form'

= link_to t("upload_photos"), cabinet_new_car_photo_path(@car.title)
\|
= link_to t("show"), @car
\|
= link_to t("all_cars"), cabinet_cars_path

- content_for :leftbar do
  = render :partial => 'leftbar'

- content_for :rightbar do
  = render :partial => 'rightbar'

