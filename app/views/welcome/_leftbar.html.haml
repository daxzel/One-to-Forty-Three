left-
-if user_signed_in?
  %p Signed in as user
  = current_user.email
  =link_to "Sign out"
  
-else
  %p Email
  %input
  %p Password
  =password_field_tag
  %p You must sing in  
%p= render :partial => "cabinet"
