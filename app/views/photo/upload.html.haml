%p= t("upload_photos")

- form_for :photo, :url => upload_photo_data_path do |item|
  %p= item.file_field :hello
  %p= submit_tag t("submit_upload")

- content_for :leftbar do
  = render :partial => 'leftbar'

- content_for :rightbar do
  = render :partial => 'rightbar'

